/* 
Coercion:- whenever we do operationn based on the input we can actually convert the inpurt for the operation
- we can convert type of input.
 
TypeCasting or type conversion.
This conversion can be manually done by us is called explicit conversion
and the conversion is done on some certain rules automatically converts the types is called implicit conversion.

Abstract operations:- These are some seta of algorithms that is present in the ecmascripts does they are not available for useage on ecmascripts.
i.e- we developers cannot use these operations directly.

*/